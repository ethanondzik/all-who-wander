using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoorClose : StateMachineBehaviour {

    public override void OnStateEnter(Animator animator, AnimatorStateInfo state, int layer) {
        // used to enable a door's collider in order to prevent a player from walking through it
    }
    
}
